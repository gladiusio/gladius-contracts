language: node_js

node_js:
  - "8.0.0"

before_install:
  - npm install -g npm@latest
  - npm install -g truffle
  - npm install -g ganache-cli

script:
  - ganache-cli
  - truffle test --network travisci
